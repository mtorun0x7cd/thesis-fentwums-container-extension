# Python bytecode cache
__pycache__/
*.pyc

# Published .NET strategy backend (build artifact)
harness_bin/

# Leftover toolchain artifacts in local_tests are produced by benchmark runs;
# results/ IS committed (the thesis cites it).
